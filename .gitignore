__pycache__/
*.pyc
.pytest_cache/
.venv/
dist/
*.egg-info/
.DS_Store
# local scratch runs; reference/community runs are committed deliberately
runs/local/

# continuous stack-watch state (history may be published selectively)
runs/watch/state.json
runs/watch/last-tick.json
runs/watch/*.tmp

# internal dry-run fixtures (public template is configs/example.json + cases/default/)
configs/dryrun-*.json
cases/dryrun-*/

# benchmark scratch
.benchmarks/
